import pr from 'pr-unit';

const paddings = {
  small: 20 * pr,
  medium: 30 * pr,
  large: 40 * pr,
  bigger: 80 * pr,
  body: 60 * pr,
};

const fonts = {
  paragraph: 40 * pr,
  subtitle: 50 * pr,
  title: 60 * pr,
};

const radius = {
  small: 20 * pr,
  medium: 40 * pr,
  large: 60 * pr,
};

const icons = {
  small: 20 * pr,
  medium: 50 * pr,
  large: 70 * pr,
};

const gap = {
  small: 20 * pr,
  medium: 40 * pr,
  large: 60 * pr,
  body: 80 * pr,
};

export default {paddings, fonts, radius, icons, gap};
